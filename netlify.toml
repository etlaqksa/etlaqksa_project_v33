[build]
  publish = "."
  command = ""

[build.processing]
  # Force Netlify to parse HTML for forms during post-processing
  # even if the UI/database flag is out of sync.
  ignore_html_forms = false
